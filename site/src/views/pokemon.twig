<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">

    <title>{{   aPage.srtTitle  }}</title>
    <meta name="description" content={{  aPage.srtDescription    }}>

</head>

<style type="text/css">
    body {
        height: 100vh;
        background: blue;
    }

    .namePokemon {
        display: flex;
        flex-direction: row;
        justify-content: center;
    }

    .namePokemon h1 {
        color: black;
        background: lightsalmon;
        padding: 5px 40px;
        border-radius: 0px 20px 20px 0px;
        transform: translateX(-30px);
        z-index: -5;
    }

    .type {
        display: flex;
        flex-direction: row;
        justify-content: center;
    }

    .type h3 {
        background: lightblue;
        border-radius: 6px;
        margin: 10px;
        padding: 10px;
    }

    .id {
        display: flex;
        justify-content: center;
        align-content: center;
        align-items: center;
        width: 25px;
        height: 25px;
        background: lightpink;
        border-radius: 50%;
        padding: 20px;
        margin: 10px;
    }

    .description {
        display: flex;
        flex-direction: column;
        justify-content: center;
    }

    .container {
        background: orange;
        display: flex;
        flex-direction: row;
        justify-content: space-around;
        height: auto;
    }

    .stats {
        background: red;
    }
</style>

<body>
    <center>
        <h2>{{  aPage.srtDescription    }}</h1>
            <div class="container">
                <div class="description">
                    <div class="namePokemon">
                        <h3 class="id">{{  aPokemon.id    }}</h3>
                        <h1>{{  aPokemon.name   }}</h1>
                    </div>

                    <img src="/assets/images/{{ '%03d' | format(aPokemon.id) }}.png" alt="{{ aPokemon.name }}"
                        width="25%" height="25%">

                    <div class="type">
                        <h3>{{  aPokemon.type1    }}</h3>
                        <h3>{{  aPokemon.type2    }}</h3>
                    </div>
                </div>
                <div class="stats">
                    <div class="points">
                        <h4>Ataque</h4>
                    </div>
                </div>
            </div>

    </center>
</body>

</html>